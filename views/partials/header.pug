html
    head
        title= title
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel='stylesheet', href='/stylesheets/style.css')
    body
        header
            h1= "Library Application"
            ul(class='nav')
                each val in ['Home', 'About', 'Items', 'Contact', 'FAQ']
                    li= val
        div.container
            block content